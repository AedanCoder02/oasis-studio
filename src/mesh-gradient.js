// --- src/mesh-gradient.js ---
// Persistent mesh gradient controller
// Uses Oasis beige/grey palette universally across all pages

/**
 * Initialize mesh gradient controller.
 * The gradient is now purely CSS-animated with fixed palette colors.
 * No per-route color changes needed — same background everywhere.
 */
export function initMeshGradient() {
  // The mesh gradient is entirely CSS-driven now.
  // This function is kept for the boot sequence compatibility.
  // All blob animations and colors are defined in mesh-gradient.css
}
